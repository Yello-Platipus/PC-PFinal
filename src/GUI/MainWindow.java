package GUI;

import javax.swing.*;

public class MainWindow extends JPanel {
    public MainWindow() {
        super();
        this.add(new JButton("No se para qué hacer esta GUI, pero bueno..."));
    }
}
